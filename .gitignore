node_modules/
dist/
.env
.env.local
*.log
.DS_Store
# Exclude everything in server/data EXCEPT the curated stock-images directory,
# which IS committed (bundled with the app). Negations only work if the parent
# isn't fully excluded — so we whitelist specific subpaths instead.
server/data/*
!server/data/stock-images/
data/
